export const ButtonEdit = ({ productId}) => {
    /* const {token} = useAuth()
    const handleClick = () => {
        deleteProductService(productId, token)
    } */
    return (
        <li>
            <button  className="pruductInfo__info__button">Editar</button>
        </li>
    )
}